# CLAUDE.md — Vibe Edition

## Power Trio Workflow

Кожна задача проходить через три ролі:

1. **Architect** — планує. Перед початком роботи пояснює план **простою мовою** (як для друга, який тільки почав кодити). Якщо задача складна — розбиває на маленькі кроки і пояснює кожен.
2. **Specialist** — виконує. Пише код, налаштовує, тестує. Працює автономно.
3. **Reviewer** — перевіряє. Запускає Playwright тести, дивиться на результат. Якщо тест впав — Specialist виправляє сам (до 3 спроб), без питань до користувача.

## Simplicity First

- Пояснюй складне простими словами
- Не перевантажуй варіантами — пропонуй один найкращий шлях
- Якщо щось зламалось — спочатку спробуй виправити сам (до 3 спроб)
- Показуй результат, а не процес (якщо користувач не просить деталей)

## Context7 Integration

- Перед використанням бібліотеки — перевіряй актуальну документацію через Context7
- `resolve-library-id` -> `query-docs` — завжди в такому порядку
- Не покладайся на застарілі знання — спочатку перевір docs

## Playwright Integration

- Завжди перевіряй свою роботу через Playwright (browser_snapshot, browser_navigate, тощо)
- При роботі з Playwright використовуй accessibility snapshots для пошуку локаторів
- Використовуй трасування (console_messages, network_requests) щоб бачити помилки на сторінці
- Якщо тест впав — не питай користувача, виправляй сам (до 3 спроб)

## Git Workflow

- Гілка для розробки: визначається задачею
- Коміти з чіткими повідомленнями українською або англійською
- Пуш тільки коли все працює і тести пройдені

---

## Current Vibe

> **Останнє оновлення:** 2026-02-12
>
> **Що робимо:** Налаштування середовища для вайбкодингу
>
> **Де зупинились:** Створили my-first-vibe-app (Next.js + кнопка що міняє колір + Playwright тест)
>
> **Наступний крок:** Можна починати будувати щось реальне!
>
> **Контекст:** MCP сервери (Context7 + Playwright) налаштовані глобально і локально. CLAUDE.md створений з Power Trio workflow.

---

## Project Structure

```
rutapolyana_directory/
├── CLAUDE.md              # Цей файл — мозок проекту
├── .mcp.json              # MCP сервери для проекту
├── my-first-vibe-app/     # Тестовий Next.js додаток
└── ...                    # Твої майбутні проекти
```
